var initialState = [];

const ChapterReducer = (state = initialState, action) => {
    switch (action.type) {
        default:
            return [...state];
            break;
    }
}

export default ChapterReducer;